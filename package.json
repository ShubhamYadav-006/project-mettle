{
  "name": "mettle-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Mettle Life RPG Platform",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  }
}
